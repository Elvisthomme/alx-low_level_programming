/**
 * _abs - compute the absolute value of an interger
 * @number: an interger number
 * Return: The absolute value of the number
 */
int _abs(int number)
{
	if (number >= 0)
		return (number);
	else
		return (-number);
}
